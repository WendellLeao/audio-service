# Changelog

All notable changes to this package are documented in this file.

## [1.0.0] - 2026-08-16

### Added

- Initial release: `AudioService` with `PlaySound`, drawing `SoundPlayer` instances from a `PoolingService` pool.
- `AudioData` and `AudioDataCollection` ScriptableObjects for configuring sounds.
- `IAudioService` abstraction.
